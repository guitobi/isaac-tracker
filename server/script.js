const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const runs = await prisma.run.findMany({ take: 5, orderBy: { id: 'desc' } }); console.log(runs.map(r => r.duration)); } main().finally(() => prisma.$disconnect());
